node_modules
dist
dist-ssr
.DS_Store
*.local
*.log
npm-debug.log*
.env
.env.*
!.env.example
.vite
coverage

# Native shells (generated by Capacitor, not source)
/android
/ios

# Fetched feed payloads are committed on purpose (the cron job updates them),
# but the raw scratch cache is not.
/.news-cache
